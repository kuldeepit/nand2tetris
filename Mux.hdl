/** 
 * Multiplexor:
 * out = a if sel == 0
 *       b otherwise
 */
// a = 0; b = 1; sel = 0;

CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    Not(in=sel,out=notsel);
    And(a=b,b=notsel,out=anotsel);
    And(a=a,b=sel,out=bsel);
    Or(a=anotsel,b=bsel,out=out);
}